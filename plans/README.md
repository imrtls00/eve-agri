# Implementation Plans

Generated by the improve skill on 2026-07-15. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish test baseline with Vitest | P1 | M | — | TODO |
| 002 | Wire real data path from API to dashboard | P1 | S | 001 | TODO |
| 003 | Externalize firmware config to gitignored header | P2 | S | — | TODO |
| 004 | Clean up dead code, deduplicate threshold logic | P3 | S | — | TODO |
| 005 | Populate DESIGN.md and restructure dashboard | P1 | M | — | DONE |

## Dependency notes

- **002 requires 001** because plan 002 modifies the data fetching layer (the riskiest change in the set) and no test exists to verify the data path. Characterization tests for the current mock-data behaviour must land first so the real-data transition has a regression safety net.

## Findings considered and rejected

- **Duplicate shadcn dependency in `dependencies` vs `devDependencies`**: trivial `npm install --save-dev shadcn && npm uninstall shadcn` but the package is a CLI tool that effectively no-ops at runtime; its placement has no behavioral impact. Not worth its own plan — included as a note in plan 004.
- **HTTPS for firmware API URL**: the firmware already references `http://your-dashboard` (placeholder). In a real deployment the URL will be configured per-environment; forcing HTTPS in the placeholder adds a bootstrap complication for local dev. Documented in CONTEXT.md as a deployment-time concern.
- **Module-level mutable state in mock-data.ts**: the `let t` variable is a test-only helper that only affects mock timestamps — not used in production. The test plan in 002 will cover it. Not worth a standalone plan.
